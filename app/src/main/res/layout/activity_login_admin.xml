<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    tools:context=".LoginAdmin">>

    <ImageView
        android:id="@+id/ivLogo"
        android:layout_width="176dp"
        android:layout_height="201dp"
        android:layout_marginTop="100dp"
        android:layout_centerHorizontal="true"
        app:srcCompat="@drawable/estacionamientologo" />

    <TextView
        android:id="@+id/tvTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/ivLogo"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="210dp"
        android:text="Acceso Admin"
        android:textSize="28sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/etCorreoAdmin"
        android:layout_width="230dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvTitulo"
        android:layout_marginTop="25dp"
        android:layout_centerHorizontal="true"
        android:ems="11"
        android:hint="Correo Electrónico"
        android:inputType="textEmailAddress"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/etPasswordAdmin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etCorreoAdmin"
        android:layout_marginTop="31dp"
        android:layout_centerHorizontal="true"
        android:ems="11"
        android:hint="Contraseña"
        android:inputType="textPassword" />

    <Button
        android:id="@+id/btIniciarAdmin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/etPasswordAdmin"
        android:layout_marginTop="65dp"
        android:layout_centerHorizontal="true"
        android:text="Iniciar Sesión Admin" />




</RelativeLayout>